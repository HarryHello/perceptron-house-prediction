# Artificial Intelligence
## 人工智能实验
**中山大学计算机学院 2026年春季**
### 机器学习基础-感知机

---

## 目录
1. [理论课内容回顾](#1-理论课内容回顾)
    1.1 [人工神经网络介绍](#11-人工神经网络介绍)
2. [实验任务](#2-实验任务)
    2.1 [购房预测分类任务](#21-购房预测分类任务)
3. [作业提交说明](#3-作业提交说明)

---

## 1. 理论课内容回顾

### 1.1 人工神经网络介绍

*   **神经网络思想**：
    *   神经网络采用了仿生学的思想，通过模拟生物神经网络的结构和功能来实现建模。

#### 单层感知机 (Perceptron)
*   由于 M-P 神经元模型参数需要事先设定好，为了能够自适应学习出所需要的参数，研究人员就提出了单层感知机 (Perceptron)。
*   **感知机的基本公式为**：
    $$ y(x) = \text{sign}(w \cdot x + b) $$
*   $\text{sign}$ 为符号函数，当自变量为正数时取值为 1，否则取值为 0。

#### 多层感知机 (Multi-Layer Perceptron, MLP)
*   **结构**：包含三个层次：一个输入层，一个或多个中间层（也叫隐藏层，hidden layer）和一个输出层。
*   **节点数**：输入层与输出层的节点数是固定的，中间层则可以自由指定。
*   **偏置单元**：MLP 通常还会引入偏置单元 $b$。
*   **前向传播示例**：
    $$ h_1 = \sigma_1(W_1 \times x + b_1) $$
    $$ y = \sigma_2(W_2 \times h_1 + b_2) = \sigma_2(W_2 \times \sigma_1(W_1 \times x + b_1) + b_2) $$

#### 激活函数
常用激活函数包括 Sigmoid、ReLU、Tanh：

*   **(a) Sigmoid**：多用于多层神经网络 (MLP)
    $$ \text{sigmoid}(x) = \frac{1}{1 + e^{-x}} $$
*   **(b) ReLU**：多用于深层神经网络 (DNN)
    $$ \text{ReLU}(x) = \max(0, x) $$
*   **(c) Tanh**：多用于深层神经网络 (DNN)
    $$ \tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}} $$

#### 损失函数
*   **作用**：为了衡量网络表现是否良好，并为之后的网络参数优化提供指导。
*   **常见的用在分类任务上的损失函数**：
    *   **均方误差 (MSE)**:
        $$ L_{MSE} = \frac{1}{2N} \sum_{i=1}^{N} (y_i(x) - \hat{y}_i(x))^2 $$
    *   **交叉熵 (Cross Entropy)**:
        $$ L_{CE} = -\frac{1}{N} \sum_{i=1}^{N} [y_i(x) \log \hat{y}_i(x) + (1 - y_i(x)) \log(1 - \hat{y}_i(x))] $$
    *(注：原文公式排版较乱，此处根据常见定义进行了标准化整理)*

#### 梯度下降
*   **梯度定义**：梯度是一个向量，表示某一函数在该点处的方向导数沿着该方向取得最大值。
    *   也就是说该点处沿着梯度的方向变化最快，变化率最大。
    *   沿着梯度方向容易找到函数最大值。
    *   沿着梯度方向的反方向，容易找到函数最小值。
*   **梯度下降的一般公式为**：
    $$ w = w - \eta \nabla_w L(w) $$
    其中，$\eta$ 是学习率，$\nabla_w L$ 是对 $w$ 的梯度，$w$ 是参数。

#### 学习率
*   学习率限制了下一步能到达的地方。
*   如果学习率太小，可能很难达到最小值。
*   如果学习率太大，则会错过最小值，无法收敛。

---

## 2. 实验任务

### 2.1 购房预测分类任务

*   **任务描述**：利用感知机算法在给定数据集完成购房预测训练。
*   **要求**:
    1.  选择合适的损失函数，利用训练集完成网络训练。
    2.  画出训练和验证集的 loss 曲线图。
    3.  包含相关的分析，判断训练是否发生过拟合。
*   **数据来源参考**：
    *   [Kaggle House Prices Advanced Regression Techniques](https://www.kaggle.com/c/house-prices-advanced-regression-techniques)

*   **算法流程示例**：
    1.  假定网络为多层感知机，网络输出为 $\hat{y} = \text{MLP}(x; \theta)$，$x$ 为房子的特征，$\text{MLP}$ 为多层神经网络，$\theta$ 为 MLP 的参数，$\hat{y}$ 为预测的房价。
    2.  设置损失函数为 $L_{MSE}$，并随机初始化网络参数。
    3.  当满足终止条件时，终止优化，否则继续。
    4.  计算网络输出 $\hat{y} = \text{MLP}(x; \theta)$，以及损失 $L_{MSE} = (\text{MLP}(x; \theta) - y)^2$，$y$ 为真实房价。
    5.  对所有网络参数求导 $\frac{\partial L}{\partial \theta}$。
    6.  根据 $\theta = \theta - \eta \frac{\partial L}{\partial \theta}$ 更新参数 $\theta$，$\eta$ 为学习率（步长）。
    7.  跳转到步骤 3。

---

## 3. 作业提交说明

*   **压缩包命名**：“学号_姓名_作业编号”，例：`20260528_张三_实验4`。
*   **提交内容本次实验报告和决策树合为一次报告提交。**
*   **作业文件下包含两部分**：
    *   `code` 文件夹：存放实验代码；
    *   实验报告 PDF 文件：格式参考发的模板。
*   **截止日期**：2026年6月7日晚24点。
*   **提交方式**：超算习堂